# Your OpenAI API key for the free tier (visitors without their own key)
OPENAI_API_KEY=sk-...

# Max free-tier API requests per IP per day (default: 50)
# A full comparison uses ~10 requests, so 50 = ~5 free comparisons/day
DAILY_FREE_LIMIT=50

# Upstash Redis (required for rate limiting)
# Create a free database at https://console.upstash.com
UPSTASH_REDIS_REST_URL=https://...
UPSTASH_REDIS_REST_TOKEN=...
